import 'package:flutter/material.dart';

class FeesContent extends StatelessWidget {
  const FeesContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
